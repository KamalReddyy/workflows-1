name: my first work flow
on: push

jobs:
    first_job:
        runs_on: ubuntu-latest

        steps:
          - name: checkout code
            uses: actions/checkout/@v4

          - name: list the files
            run: ls